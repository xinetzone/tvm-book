# Relax

**Relax** 是 Apache TVM 堆栈中用于计算图优化和变换的高级抽象。此外，Apache TVM 将 Relax 与 TensorIR 结合使用，作为跨层级优化的统一策略。因此，Relax 通常与 TensorIR 紧密协作，以表示和优化整个 IRModule。

```{toctree}
:hidden:

creation
transformation
```
