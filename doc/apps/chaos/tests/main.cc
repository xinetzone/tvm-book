#include <iostream>
// #include <vector>
#include <string.h>
// #include <sstream>
// #include <cassert> // 断言
// // #include <random>
// #include <algorithm>
// #include <string_view>
// #include <type_traits>
// #include <cmath>
#include <tvm/runtime/c_runtime_api.h>
#include <tvm/runtime/packed_func.h>
#include <tvm/runtime/object.h>
#include <tvm/runtime/registry.h>
#include <tvm/runtime/logging.h>
// using namespace tvm::runtime;

namespace tvm {
namespace runtime {

// void TestObjects() {
//     // 创建对象
//     // ObjectRef leaf_ref(make_object<BaseNode>());
//     // 转换为特定实例
//     // const LeafObj* leaf_ptr = leaf_ref.as<LeafObj>();
//     // ICHECK(leaf_ptr != nullptr);
//     // 也可以转换为基类
//     // ICHECK(leaf_ref.as<BaseObj>() != nullptr);
// }
}
}

int main(void)
{
    int a = 1;
    // tvm::runtime::TestObjects();
}